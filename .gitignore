# Common
node_modules
.env
.DS_Store
dist
build
coverage

# Flutter
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.packages
.pub-cache/
.pub/

# Render-specific: We want the repo to contain everything, but we can use .slugignore for Render optimization if needed. 
# However, usually Render just builds what's in 'rootDir'. 
# The user asked to "modify git", likely meaning to avoid committing heavy/unnecessary files.
# We will keep mobile_flutter tracked but ignore its build artifacts.

mobile_flutter/build/
mobile_flutter/windows/flutter/ephemeral/
mobile_flutter/android/.gradle
